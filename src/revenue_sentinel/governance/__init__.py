"""Layer 4 -- deterministic policy engine, risk tiers, approvals.

A policy layer you cannot prove is not a policy layer (ADR-0005), so every
decision is a pure function of its inputs and is recorded.
"""
